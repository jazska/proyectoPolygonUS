# Requisitos para entrenar el modelo de detección de fake news con BERT
# Este archivo incluye las librerías necesarias para ejecutar el notebook en Google Colab o localmente con GPU

# Librerías principales de NLP y entrenamiento
transformers==4.33.2         # Modelos preentrenados como BERT, tokenización y entrenamiento
datasets==2.14.5             # Manejo de datasets en formato Hugging Face
accelerate==0.23.0           # Optimización de entrenamiento en GPU/TPU

# Librerías de machine learning y métricas
scikit-learn==1.3.2          # Métricas como accuracy, F1, precision, recall
pandas==2.1.1                # Manipulación de datos tabulares
numpy==1.26.0                # Operaciones numéricas

# Librerías para entrenamiento en GPU
torch==2.1.0                 # Framework de deep learning compatible con CUDA 11.8

# Librería para subir modelos a Hugging Face
huggingface_hub==0.18.0      # Autenticación, creación de repositorios y subida de modelos

# Recomendación:
# Para instalar estas dependencias, ejecuta:
# pip install -r requirements.txt